# Infra Monitor runtime dependencies. All permissive or weak-copyleft (LGPL) —
# no AGPL, no proprietary.
paramiko>=3.0        # SSH client (LGPL-2.1)
fabric>=3.0          # SSH command orchestration (BSD)
requests>=2.28       # HTTP (external stealth/reachability checks) (Apache-2.0)
pystray>=0.19        # system-tray icon (LGPL-3.0)
Pillow>=10.4         # tray icon images (HPND)
cryptography>=41     # paramiko key crypto (Apache-2.0/BSD)
customtkinter>=5.2   # Aura design system: ttk/tk theming layer (MIT)
darkdetect>=0.8      # OS light/dark appearance + live change signal (BSD)
